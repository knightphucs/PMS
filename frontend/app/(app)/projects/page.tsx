'use client';

/** Khung tạm để kiểm chứng luồng auth end-to-end. Nội dung thật ở đợt kế tiếp. */
export default function ProjectsPage() {
  return (
    <div className="grid gap-2">
      <h1 className="text-2xl font-semibold tracking-tight">Dự án</h1>
      <p className="text-muted-foreground">Danh sách dự án sẽ hiển thị ở đây.</p>
    </div>
  );
}
