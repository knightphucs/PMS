$kind: http-request
name: Logout
url: "{{api_url}}/Auth/logout"
method: POST
body:
  type: json
  content: |-
    {
        "refreshToken": "string"
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 2996057533104410
