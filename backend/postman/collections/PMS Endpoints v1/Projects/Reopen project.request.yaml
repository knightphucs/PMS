$kind: http-request
name: Reopen project
url: "{{api_url}}/projects/:project_id/reopen"
method: POST
pathVariables:
  - key: project_id
    value: ""
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 4424360515822773
