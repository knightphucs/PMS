$kind: http-request
name: Get approval policies
url: "{{api_url}}/projects/:project_id/approval-policies"
method: GET
pathVariables:
  - key: project_id
    value: ""
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1787435414036
