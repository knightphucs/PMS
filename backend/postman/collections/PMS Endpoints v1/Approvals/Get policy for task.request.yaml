$kind: http-request
name: Get policy for task
url: "{{api_url}}/tasks/:task_id/approvals"
method: GET
pathVariables:
  - key: task_id
    value: ""
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 4503580850511091
