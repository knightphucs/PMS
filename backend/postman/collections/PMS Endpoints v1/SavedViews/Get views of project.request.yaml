$kind: http-request
name: Get views of project
url: "{{api_url}}/projects/:project_Id/views"
method: GET
pathVariables:
  - key: project_Id
    value: ""
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1787437154972
