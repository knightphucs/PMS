$kind: http-request
name: List watchers
description: "Danh sách người đang theo dõi task. Quyền View nên Viewer cũng đọc được."
url: "{{api_url}}/tasks/:task_id/watchers"
method: GET
pathVariables:
  - key: task_id
    value: 8DD5602C-EB76-4E72-BFF4-707011B0E6AF
    description: Task liên quan
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1785500008000
