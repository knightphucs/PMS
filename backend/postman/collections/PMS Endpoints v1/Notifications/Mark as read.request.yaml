$kind: http-request
name: Mark as read
description: "Đánh dấu một thông báo đã đọc. Idempotent: gọi lần hai vẫn 200, không phải 409 (ADR-023). Thông báo của người khác trả 404 chứ không 403."
url: "{{api_url}}/notifications/:id/read"
method: PATCH
pathVariables:
  - key: id
    value: ""
    description: Id thông báo, copy từ response GET /notifications
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 4475439156213327
