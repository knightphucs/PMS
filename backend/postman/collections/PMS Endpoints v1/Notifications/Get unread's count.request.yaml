$kind: http-request
name: Get unread's count
description: "Số thông báo chưa đọc — endpoint riêng để badge trên chuông không phải tải cả danh sách."
url: "{{api_url}}/notifications/unread-count"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 4443047844779906
