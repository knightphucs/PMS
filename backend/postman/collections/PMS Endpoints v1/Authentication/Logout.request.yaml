$kind: http-request
name: Logout
url: "{{api_url}}/auth/logout"
method: POST
body:
  type: json
  content: |-
    {
        "refreshToken": "string"
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 3391370548275651
