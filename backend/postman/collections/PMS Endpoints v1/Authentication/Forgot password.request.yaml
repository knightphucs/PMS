$kind: http-request
name: Forgot password
description: "Yêu cầu đặt lại mật khẩu (ADR-041).\n\nLUÔN trả 204, kể cả khi email không tồn tại — đó là CHỦ ĐÍCH, không phải lỗi: phân biệt được hai trường hợp là biến endpoint này thành công cụ dò xem ai đã đăng ký.\n\nMôi trường Development ghi email giả lập ra log (console + logs/pms-*.log) — lấy mã đặt lại từ đó. Rate limit 3 lần/phút theo IP -> có thể nhận 429."
url: "{{api_url}}/auth/forgot-password"
method: POST
body:
  type: json
  content: |-
    {
        "email": "an@pms.local"
    }
auth:
  type: noauth
order: 1785600001000
