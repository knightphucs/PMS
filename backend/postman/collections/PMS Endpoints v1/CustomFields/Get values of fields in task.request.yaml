$kind: http-request
name: Get values of fields in task
url: "{{api_url}}/tasks/:task_id/field-values"
method: GET
pathVariables:
  - key: task_id
    value: ""
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 3247936432659161
