$kind: http-request
name: Get velocity
url: "{{api_url}}/projects/:project_id/reports/velocity"
method: GET
pathVariables:
  - key: project_id
    value: ""
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 3472444289346247
