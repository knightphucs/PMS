$kind: http-request
name: Get token invite
url: "{{api_url}}/invitations/:token"
method: GET
pathVariables:
  - key: token
    value: ""
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1787436620587
