$kind: http-request
name: Delete comment
description: "Xóa bình luận — tác giả HOẶC ProjectManager (ADR-026). Xóa CỨNG, không phải xóa mềm; audit trail do ActivityLog đảm nhiệm."
url: "{{api_url}}/comments/:id"
method: DELETE
pathVariables:
  - key: id
    value: ""
    description: Id bình luận, copy từ response GET comments
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 2290543099642350
