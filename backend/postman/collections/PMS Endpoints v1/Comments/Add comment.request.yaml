$kind: http-request
name: Add comment
description: "Viết bình luận trên task. ProjectManager/Member viết được; Viewer nhận 403 (§10). Sinh thông báo CommentAdded cho assignee + watcher + reporter."
url: "{{api_url}}/tasks/:task_id/comments"
method: POST
pathVariables:
  - key: task_id
    value: 8DD5602C-EB76-4E72-BFF4-707011B0E6AF
    description: Task cần bình luận
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
        "content": "Phần này em làm xong rồi, anh review giúp em nhé"
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1785404050144
