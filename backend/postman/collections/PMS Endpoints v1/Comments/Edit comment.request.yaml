$kind: http-request
name: Edit comment
description: "Sửa bình luận — CHỈ tác giả (ADR-026). ProjectManager cũng nhận 403: xóa lời người khác là kiểm duyệt hợp lý, viết lại lời người khác thì không vì nội dung vẫn đứng tên tác giả cũ."
url: "{{api_url}}/comments/:id"
method: PUT
pathVariables:
  - key: id
    value: ""
    description: Id bình luận, copy từ response POST hoặc GET comments
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
        "content": "Đã sửa lại nội dung bình luận"
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 2011707590373862
