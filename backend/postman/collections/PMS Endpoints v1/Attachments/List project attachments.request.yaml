$kind: http-request
name: List project attachments
description: "File đính kèm cấp project."
url: "{{api_url}}/projects/:project_id/attachments"
method: GET
pathVariables:
  - key: project_id
    value: 2C1A5A6E-7F3B-4E10-9A44-4C1D2E5F6A70
    description: Project liên quan
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1785500019000
