$kind: http-request
name: List task attachments
description: "Quyền View nên Viewer cũng xem và tải được."
url: "{{api_url}}/tasks/:task_id/attachments"
method: GET
pathVariables:
  - key: task_id
    value: 8DD5602C-EB76-4E72-BFF4-707011B0E6AF
    description: Task liên quan
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1785500018000
