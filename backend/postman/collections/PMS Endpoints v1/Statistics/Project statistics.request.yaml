$kind: http-request
name: Project statistics
description: "Tổng hợp trong SQL. byStatus LUÔN đủ 4 phần tử và byPriority đủ 5, kể cả count = 0 — client không phải tự bù. Đếm CẢ subtask."
url: "{{api_url}}/projects/:project_id/statistics"
method: GET
pathVariables:
  - key: project_id
    value: 2C1A5A6E-7F3B-4E10-9A44-4C1D2E5F6A70
    description: Project liên quan
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1785500026000
