$kind: http-request
name: Unlock an user
url: "{{api_url}}/admin/employees/:id/unlock"
method: POST
pathVariables:
  - key: id
    value: cc7a6f95-251c-4d72-90cd-d397b70aadbc
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 3611706922644686
